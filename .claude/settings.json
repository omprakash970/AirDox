{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; if [ -f \"$f\" ]; then npx prettier --write \"$f\" 2>/dev/null; fi; } || true",
            "statusMessage": "Formatting with Prettier..."
          }
        ]
      }
    ]
  }
}
